package com.revature.nonaccessmods;

public class NonAccessModifiers {

	/*
	 * Examples of non access modifiers in Java
	 * 
	 * static
	 * The member belongs to the class, not to the objects of the class
	 * 
	 * final
	 * Variable values can't be changed
	 * 	If you make a variable final, write it in ALL CAPS
	 * Methods can't be overridden
	 * Classes cannot be inherited
	 * 
	 * abstract
	 * If applied to a method, it has to be implemented by a sub class
	 * If applied to a class, it contains abstract methods, and cannot 
	 * be instantiated on its own
	 * 
	 * Synchronized
	 * Controls thread access to a block / method
	 * 
	 * Volatile
	 * Transient
	 */
	
	public static void main(String[] args) {
		
	}
}
